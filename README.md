Simple docker container to run AWS lambda environment on your local machine

# To build the image
`docker build -t docker-lambda .`

# To run the image and access bash
`docker run -it --name docker-lamb docker-lambda bash`
